
import { GravityText } from "@/components/GravityText";

const Index = () => {
  return (
    <div className="min-h-screen bg-terminal-bg font-mono text-terminal-text">
      <GravityText text="Welcome to the UNIX Terminal" />
    </div>
  );
};

export default Index;
